class Administrator {
  constructor(id, firstName, lastName, userName, password, surveys) {
    (this.id = id),
      (this.firstName = firstName),
      (this.lastName = lastName),
      (this.userName = userName),
      (this.password = password),
      (this.surveys = surveys);
  }
}
