class UserProduct {
  constructor(id, userid, productId, position, survey) {
    (this.id = id),
      (this.userid = userid),
      (this.productId = productId),
      (this.position = position),
      (this.survey = survey);
  }
}
