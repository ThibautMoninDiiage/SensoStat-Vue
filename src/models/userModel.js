class User {
  constructor(id, surveyId, code, link, answers, userProducts) {
    (this.id = id),
      (this.surveyId = surveyId),
      (this.code = code),
      (this.link = link),
      (this.answers = answers),
      (this.userProducts = userProducts);
  }
}
