class Question {
  constructor(id, libelle, position, surveyId, answers) {
    (this.id = id),
      (this.libelle = libelle),
      (this.position = position),
      (this.surveyId = surveyId),
      (this.answers = answers);
  }
}
